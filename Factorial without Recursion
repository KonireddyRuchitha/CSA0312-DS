#include<stdio.h>
int main()
{
    int a,i,fact=1;
    printf("enter a number= ");
    scanf("%d", & a);
    for(i=1;i<=a;i++)
    {
        fact=fact*i;
    }
    printf("%d factorial is %d",a,fact);
}


OUTPUT:
enter a number= 8
8 factorial is 40320
